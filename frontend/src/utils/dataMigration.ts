/**
 * Data Migration — Stubbed
 *
 * Firebase Firestore migration utilities have been removed.
 * Club data is now managed via Supabase. This file is kept as a
 * stub so existing imports don't break.
 */

export const migrateAllData = async () => {
  console.warn('dataMigration.migrateAllData: Firestore removed, use Supabase admin panel');
};

export const migrateClubs = async () => {
  console.warn('dataMigration.migrateClubs: Firestore removed');
};
