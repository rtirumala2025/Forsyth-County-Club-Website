/**
 * Migrate Club Data — Stubbed
 *
 * Firebase Firestore migration script has been removed.
 * Club data is now managed via Supabase.
 */

export const migrateClubDataToFirestore = async () => {
  console.warn('migrateClubData: Firestore removed. Use Supabase admin panel to seed club data.');
};
